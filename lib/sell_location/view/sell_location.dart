import 'package:flutter/material.dart';

class SellLocation extends StatelessWidget {
  const SellLocation({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('SellLocation',style: TextStyle(fontSize: 20.0),)),
    );
  }
}
